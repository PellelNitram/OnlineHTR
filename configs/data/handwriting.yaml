_target_: src.data.online_handwriting_datamodule.SimpleOnlineHandwritingDataModule
data_dir: ${paths.data_dir}/datasets/2024-01-20-xournal_dataset.xoj
batch_size: 128 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
train_val_test_split: [1, 0, 0]
num_workers: 0
pin_memory: False
